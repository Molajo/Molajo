* * * * Model / DB interaction * * * *

DBO for:

a. Messages
b. Head data (CSS/JS)
c. Registry (Parameters - like head and footer and the grid)

2. Migrate Module models Model Service and above DBO's

=> Only Molajo Models will be EntryModel, DisplayModel, ItemModel, and Model

3. Parser - update for new formats

4. Includers - update for new formats (create new tag, formfields)

5. Controller(Display) - update for new formats

* * * * Services Layer (Middleware) * * * *

AssetService - JS and CSS; interface with minimizers, etc.

AuthenticationService - Events for Login, etc

FilterService - get HTML Purifier back in with namespaces

* * * * NOTES * * * *

Manifest.xml => Parameters

Remove all specifically coded paths from code - instead of FileSystem use the Asset, Configuration API, etc

Bring in the php spl priority
